spring:
    datasource:
        url: jdbc:mysql://localhost:3306/todo_website
        username: root
        password: Manisha@2285
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
              format_sql: true

    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
